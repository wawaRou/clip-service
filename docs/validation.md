# 原生运行验收

本项目按 Mac、Thor、Orin 三组固定依赖使用原生 uv 环境。自动回归使用可控的视频和
编码边界验证业务规则；实机验收使用本地真实 CLIP 模型与 Frigate 转发流，两者分别记录。

2026-09-08 解码帧驱动调度的 Thor TF32 吞吐记录见
[单路与三路并发测量](validation/2026-09-08-thor-frame-driven.md)。

TF32 / FP16 模型吞吐、多路容量，以及五图 JSON 请求的 VLM 共存测量见
[Thor 精度与共存容量](validation/2026-09-08-thor-precision-capacity.md)。

## 当前工作区验证（2026-09-08）

在 Thor 上检查 `8f2cb7f` 之后的解码帧驱动实现（每路队列 6 帧）及回执重试修复：

- 全量 pytest：121 项通过，1 项因 Thor 无 MPS 硬件跳过；包括真实小型 CLIP 权重的
  CPU、CUDA FP32/TF32/FP16 编码测试，以及 HTTP/SSE、重载、恢复和队列回归。
- 摄像头删除、禁用或重启后不再配置时，已完成候选仍可幂等回执；三个新增场景均先在
  旧实现下复现 404，再修复并通过，冲突结果仍返回 409。
- Ruff 检查与格式检查、Pyright、`uv lock --check --offline`、源码包及 wheel 构建通过；
  CLI 和手动验收脚本的帮助入口可运行，完整配置示例与内置默认值一致。
- 实验 JSON 全部可解析，报告表格与原始数据核对一致；独立环境重新生成四张 PNG，
  与保存的图逐像素一致，并生成四页 PDF。

本轮提交检查不重新施加 VLM 容量测试负载，真实性能条件与限制以上方同日实验报告为准。
Mac 的历史验证不能替代当前帧驱动实现的 MPS 实机回归；Orin 安装及原生推理仍待验证。

## 复现完整闭环与测量

先按 README 安装对应 extra，确认本机模型目录和 Frigate 转发流可用。Mac 示例：

```bash
uv run --locked --extra mac python tests/manual_native.py \
  --model '/path/to/local/clip-vit-base-patch16' \
  --device mps \
  --frigate-url rtsp://127.0.0.1:8554 \
  --stream living_room_c225_sub \
  --output data/acceptance/mac-native.json
```

Thor / Orin 分别使用 `--extra thor` / `--extra orin`，指定 `--device cuda`，并将
Frigate 地址改为该设备能够访问的主机地址。模型路径必须为完整本地 CLIP 目录；脚本
开启离线加载，不自动下载模型。服务配置支持模型 ID，但此手动验收脚本的 `--model`
仍要求目录路径，不接受模型 ID。认证沿用 `CLIP_FRIGATE_USERNAME` 和
`CLIP_FRIGATE_PASSWORD`。基准、推理使用指定后端，GPU 不可用时失败，不回退到 CPU。

脚本在临时目录写 TOML，以当前 Python 启动独立服务进程；依次验证单路、热添加至
三路、设置基准、SSE 通知、五张候选图、HTTP 对账、幂等回执以及删除摄像头的结束
通知。运行后终止服务并清理临时配置、日志及候选图，只保留指定 JSON 报告。
三路使用同一个 Frigate 流的三个逻辑摄像头，验证共享模型与并发读取负载；这不等同于
三台独立物理摄像头的现场验收。手工逐步调用接口的流程见 [接口说明](api.md)。

默认每路目标 8 FPS，每阶段预热 3 秒后测量 10 秒；可用 `--fps`、`--seconds`、
`--warmup` 调整；`--precision` 默认为 fp32，也可指定 tf32（CUDA）或 fp16（CUDA / MPS）。
吞吐阶段将相似度阈值设为 -1，避免等待候选回执暂停检测。
报告采用完成编码计数差除以本机单调时钟经过时间计算实际频率，包含整个检测循环对
吞吐的影响；每 0.2 秒采样最近完成帧的处理时长，不是每一帧的完整延迟分布。

通知阶段使用合成黑图作为基准，与真实视频画面比较，阈值为 0.9999，确认时间
0.3 秒、五帧间隔 0.075 秒。这用于稳定触发接口闭环，不用于验证真实场景的误报率。
延迟从该候选成功确认窗口的首个低相似度帧在 CLIP 主机的接收时间，算到同机 SSE
客户端收到完整事件；此前因缺帧丢弃的变化窗口不在此时间范围内。
包含确认等待、模型处理、候选写盘及通知；不包含物理摄像头采集、网络或 Frigate
转发前的延迟，不宣称测得真实画面变化的完整端到端延迟。

## 历史代码验证（2026-09-07，`f0565aa`）

- Mac 全量 pytest：104 项通过，3 项 CUDA 精度测试因本机无 CUDA 跳过；Ruff 检查、
  格式检查、Pyright、源码包和 wheel 构建全部通过。
- Hugging Face 离线缓存冒烟：完整示例解析成功，从本机默认缓存加载真实 CLIP，
  MPS 编码得到归一化的 512 维 FP32 特征；空缓存直接失败，禁止网络连接时仍通过。
- 该轮缓存加载变更在 Mac 验证；当时 Thor 的实机记录对应此前精度配置版本，见下文。
  9 月 7 日该轮未重新运行 Thor，后续验证见本页顶部的 9 月 8 日记录；Orin 仍待实机验收。

## 重构初次验收记录（2026-09-07）

- Mac 完整回归：100 项通过，无跳过；Ruff、Pyright、源码包和 wheel 构建通过。
- Thor 同步 `2201b64` 后，真实模型加载、服务启动、原生视频超时等 7 项平台测试通过，
  无跳过；CLI 和 Pyright 通过。未修改既有远程项目。
- 真实模型：OpenAI CLIP ViT-B/16，snapshot
  `57c216476eefef5ab752ec549e440a49ae4ae5f3`，float32。
- Mac：Apple M2 Max、macOS 26.4.1、Python 3.12.13、torch 2.13.0、MPS。
- Thor：L4T R39.2、Python 3.12.3、torch 2.14.0+cu132、torchvision 0.29.0+cu132、
  CUDA 13.2；真实 CUDA 编码通过。
- Orin：用户确认当前不可用。保留指定版本与来源，尚未验证此项目在 Orin 的原生安装、
  CUDA 编码、真实流闭环及检测性能；设备可用后按上面的命令补齐。

## 初次验收的 Frigate 真实流结果与默认值

输入来自同一 Frigate 转发流：1280×720 H.264，ffprobe 的 `r_frame_rate=30/1`；
Thor 同时报告 `avg_frame_rate=25/1`，这些是探测字段，不保证源始终提供固定帧率。
服务以实际收到的新帧为准。以下为精度选项加入前、FP32 权重模式的历史结果，
不是当前代码各精度的统一对比基准。默认 8 FPS 的当时短测结果：

| 设备 | 逻辑路数 | 各路实际 FPS | 编码 p95（ms） | 收帧到编码 p95（ms） | 候选通知（s） |
| --- | ---: | --- | ---: | ---: | ---: |
| Mac M2 Max | 1 | 8.00 | 43.6 | 107.7 | 0.457 |
| Mac M2 Max | 3 | 7.84 / 8.04 / 8.04 | 27.8–35.5 | 82.3–127.8 | 0.427–0.460 |
| Thor | 1 | 7.92 | 65.5 | 140.5 | 0.375 |
| Thor | 3 | 7.86 / 7.86 / 7.96 | 40.1–40.8 | 107.3–135.7 | 0.448–0.524 |

编码与收帧延迟为每路抽样统计，三路栏显示各路 p95 的范围。每阶段每路只触发一次
候选，通知列是这些单次结果，不是通知延迟的 p95 或上限；全部五张图、对账、重复
回执和摄像头删除事件均通过。8 FPS 测试所有设备各路均无 stale 健康采样。

先前 10 FPS 测试中，Mac 单路为 9.68 FPS，三路为 9.77 / 9.97 / 9.87；
Thor 单路为 9.90 FPS，三路为 9.29 / 9.09 / 9.09。Mac 的 10 FPS 单路阶段出现过
一次 stale 健康采样。Thor 三路的编码 p95 约每路 39 ms，三路合计已超过 100 ms
检测周期，因此没有把 10 FPS 继续作为全局默认。

当前默认每路 8 FPS，确认时间 0.3 秒，五帧间隔 0.075 秒；在上述历史三路条件下实际
频率更接近目标，也为调度留出余量。单路或更快配置可覆盖为 10 FPS，并观察实际
FPS、帧龄和处理时长。增加摄像头后重新测量，不承诺路数或负载变化后仍满足原频率。
这些结果支持本机收帧后争取 1 秒内通知；实际误报率、摄像头到 CLIP 的延迟与长时间
稳定性仍需现场观察，默认相似度阈值 0.9 尚未做场景标定。

原始报告：[Mac 8 FPS](validation/2026-09-07-mac-8fps.json)、
[Thor 8 FPS](validation/2026-09-07-thor-8fps.json)、
[Mac 10 FPS](validation/2026-09-07-mac-10fps.json)、
[Thor 10 FPS](validation/2026-09-07-thor-10fps.json)。

Thor 本次创建的独立测试目录、虚拟环境、代码及报告已全部删除，SSH 再次确认目录
不存在且无测试进程残留；结果已保存到本地。现成模型缓存没有作为测试目录的一部分创建。

## 历史模型编码参考（2026-09-07）

同一张 640×360 灰色 JPEG（quality 85），预热 10 次后连续编码 100 次的补充测量：

| 设备 | 编码次数/秒 | 编码 p50 | 编码 p95 |
| --- | ---: | ---: | ---: |
| Mac M2 Max / MPS | 63.51 | 15.74 ms | 16.26 ms |
| Thor / CUDA | 43.04 | 24.11 ms | 24.99 ms |

这仅包含图像预处理、CLIP 推理和特征输出，没有读流、摄像头调度、候选写盘与通知，
不能当作实际检测 FPS 或设备理论上限。它也不足以支持先前“总计 100 FPS”的预估。

## 可选精度验证（2026-09-07）

新增 `model.precision` 后，Thor 实机通过 FP32、TF32、FP16 编码器测试；Mac MPS
通过 FP16 编码器测试，确认实际权重类型和归一化的 FP32 输出。Orin 尚未实机验证。

Thor 的 TF32、FP16 另以真实 Frigate 流分别完成单路、三路逻辑摄像头验收，包含
HTTP 配置传递、五张候选图、SSE 通知、幂等回执和移除摄像头。目标每路 10 FPS，
每阶段预热 2 秒、测量 5 秒：TF32 三路实际为 10.08 / 9.69 / 9.88 FPS；FP16
为 9.68 / 9.68 / 9.68 FPS。全部候选的本机收帧至 SSE 延迟为 0.35–0.47 秒。

这仍是同一物理流的三个逻辑接入、黑色基准触发的短时功能测试，不代表真实场景下
不同精度的判断一致性或性能上限。精度默认保持 FP32，目标 FPS 默认保持 8。
本次 Thor 临时测试目录（含虚拟环境和输出）已删除，报告已保存到本地。
