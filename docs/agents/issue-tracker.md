# 任务跟踪

本项目使用本地任务文件，由用户明确选择；任务位于 `.scratch/clip-refactor/issues/`。
每项独立文件，使用编号和标题记录阻塞依赖。`ready-for-agent` 表示已准备好实施，
仍需先完成其阻塞任务；实施中使用 `in-progress`，验收完成后使用 `done`。

按当前分支逐项实施，每项完成验证后单独提交 Git。评审以该项开始前的提交为基准，
规范依据用户要求的简单、可读、职责清晰及既有 ADR，需求依据对应任务的验收清单。
